# 📖 Kunpeng Novel เว็บไซต์นิยายภาษาไทย - ฟีเจอร์ทั้งหมด

## 🧭 ภาพรวม

เว็บไซต์นิยายออนไลน์ สำหรับผู้อ่าน นักแปล และแอดมิน มีระบบมาร์กบรรทัด นิยายหลายภาษา รองรับมือถือ พร้อมระบบล็อกอินและการกำหนดสิทธิ์แบบละเอียด

---

## 🔐 ระบบล็อกอินและจัดการผู้ใช้ (Authentication)

### ระบบล็อกอิน (ใช้ Supabase Auth)

* ต้องล็อกอินก่อนใช้งานทุกหน้า (ยกเว้น login / register)
* รองรับ:

  * อีเมล + รหัสผ่าน
  * Google Login (OAuth)
  * ลืมรหัสผ่าน: ส่งลิงก์ผ่านอีเมล

### การสมัครสมาชิก

* ผู้ใช้สมัครเองได้
* บัญชีใหม่จะได้รับสิทธิ์เริ่มต้นเป็น `reader`

### สิทธิ์ผู้ใช้งาน (Roles)

| สิทธิ์   | หน้าแรกหลังล็อกอิน  | สิทธิ์ที่ได้รับ                                        |
| -------- | ------------------- | ------------------------------------------------------ |
| `reader` | `/novels`           | อ่านนิยาย, บุ๊คมาร์ก, มาร์กบรรทัด                      |
| `vip`    | `/novels`           | อ่านตอน VIP, อ่านล่วงหน้า                              |
| `editor` | `/editor-dashboard` | เข้าหน้าแปล, แก้ไขต้นฉบับ/แปล, ดูสถานะงานแปล           |
| `admin`  | `/admin-dashboard`  | จัดการผู้ใช้, เปลี่ยน role, ตั้ง VIP, CRUD นิยายและตอน |

### ระบบ VIP

* แอดมินเป็นผู้ตั้งค่า:

  * `is_vip = true`
  * `vip_expires_at = 'YYYY-MM-DD'`
* ถ้าตอนถูกตั้งว่า "เฉพาะ VIP" จะตรวจว่า:

  * ผู้ใช้งานมี `is_vip = true` และ `vip_expires_at > วันนี้`

---

## 🧭 หน้าใช้งานและเมนู (Navigation)

### หน้าทั่วไป (ทุกคนเข้าถึงได้หลังล็อกอิน)

| หน้า         | URL                        | คำอธิบาย                      |
| ------------ | -------------------------- | ----------------------------- |
| ล็อกอิน      | `/login`                   | หน้าเข้าสู่ระบบ               |
| สมัครสมาชิก  | `/register`                | หน้าเปิดบัญชีใหม่             |
| ลืมรหัสผ่าน  | `/forgot-password`         | รีเซ็ตรหัสผ่าน                |
| รายชื่อนิยาย | `/novels`                  | หน้าแสดงนิยายทั้งหมด          |
| อ่านตอนนิยาย | `/novel/<id>/chapter/<id>` | อ่านนิยาย + มาร์กบรรทัด       |
| โปรไฟล์      | `/profile`                 | ข้อมูล role บัญชี, ออกจากระบบ |

### Navbar ตามสิทธิ์

| สิทธิ์ | รายการเมนู                                                    |
| ------ | ------------------------------------------------------------- |
| reader | หน้าแรก, บุ๊คมาร์กของฉัน, สมัคร VIP, โปรไฟล์                  |
| vip    | เช่นเดียวกับ reader                                           |
| editor | หน้าแรก, หน้าต่างนักแปล, โปรไฟล์                              |
| admin  | หน้าแรก, หน้าต่างแอดมิน, จัดการผู้ใช้, เครื่องมือแปล, โปรไฟล์ |

---

## 📚 ฟีเจอร์นิยาย

### หน้านิยายหลัก (`/novel/<id>`)

* แสดงข้อมูล:

  * ชื่อจีนต้นฉบับ / ชื่อแปลไทย
  * ผู้เขียน / ผู้แปล
  * คำโปรย, หมวดหมู่, แท็ก, สถานะ (ยังแปลอยู่ / จบแล้ว / พักไว้)
  * ตอนล่าสุด 10 ตอน พร้อมวันที่
  * ปุ่มแสดงช่วงตอน: 1–50, 51–100, ...

### หน้าอ่านตอน (`/novel/<id>/chapter/<id>`)

นิยายแต่ละเรื่องสามารถกำหนดได้ว่าให้ใครเข้าถึงได้บ้าง:

* 📖 `reader` เท่านั้น
* ✍️ `editor` เท่านั้น
* 🌐 ทั้งสองกลุ่ม

### การกำหนดสิทธิ์การเข้าถึงและโหมด (ตอนสร้างนิยาย):

* ✅ กำหนดสิทธิ์การเข้าถึงนิยายด้วย **Checkbox หลายตัวเลือก**:

  * `reader`
  * `vip`
  * `editor`
* ✅ ระบบจะอนุญาตให้เฉพาะผู้ที่มี role ตรงกับ checkbox เท่านั้นเข้าอ่านได้
* ✅ ตั้งค่า "โหมด editor" (editor mode) ได้:

  * ถ้าเปิด → เปิดฟีเจอร์มาร์กบรรทัดและปุ่มคัดลอก
  * ถ้าไม่เปิด → หน้าอ่านจะเหมือนนิยายทั่วไป

### เงื่อนไขการแสดงผลหน้าตา:

#### 🔸 ถ้า **ไม่ได้เปิดโหมด editor** (นิยายทั่วไป)

* ปรับขนาดฟอนต์
* โหมดแสง / กลางคืน
* บุ๊คมาร์กตอน
* ปุ่ม “ตอนก่อนหน้า / ตอนถัดไป”
* ❌ ไม่มีระบบมาร์กบรรทัด
* ❌ ไม่มีปุ่มคัดลอกข้อความที่มาร์ก

#### 🔹 ถ้า **เปิดใช้งานโหมด editor** (Editor Mode = true)

* ปรับขนาดฟอนต์
* โหมดแสง / กลางคืน
* บุ๊คมาร์กตอน
* ปุ่ม “ตอนก่อนหน้า / ตอนถัดไป”

✅ เพิ่มฟีเจอร์พิเศษ:

* **ระบบมาร์กบรรทัด:**

  * แสดงหมายเลขบรรทัด
  * Checkbox ข้างบรรทัด → ติ๊กแล้วไฮไลต์
  * ดับเบิลคลิกเพื่อคัดลอกข้อความ (ไม่รวมหมายเลข)
  * แจ้งเตือนเล็ก ๆ มุมจอเมื่อบันทึกหรือคัดลอก
* **ปุ่ม “📋 คัดลอกข้อความที่มาร์ก”**

  * อยู่ถัดจากชื่อแต่ละตอน
  * ดึงหมายเลขบรรทัดที่มาร์ก → รวบข้อความ → คัดลอกไป clipboard

**ระบบจะตรวจสิทธิ์ของผู้ใช้ก่อนเข้า:**

* ถ้าไม่มี role ที่ถูกเลือกไว้ในการตั้งค่าของนิยาย → จะไม่สามารถเข้าถึงได้

## ✍️ โหมดนักแปล (Editor Mode)

### เส้นทางเข้าถึง

* `/editor-dashboard`: หน้าแดชบอร์ดงานแปล
* `/editor/novel/<id>`: จัดการนิยายแต่ละเรื่อง

### ความสามารถ

* เห็นรายการนิยายที่ตนเองรับผิดชอบ
* จัดการตอน:

  * ซ้าย: ข้อความจีนต้นฉบับ
  * ขวา: ช่องแปลไทย
* สถานะของตอน: draft → translated → proofread → published
* ปุ่ม: บันทึก / ส่งแก้ / เผยแพร่
* inline note toggle (ถ้ามี)
* จำกัดสิทธิ์: เห็นเฉพาะงานของตนเอง

---

## 📋 ระบบมาร์กบรรทัด (สำคัญที่สุด)

### พฤติกรรม UI

* แสดงหมายเลขบรรทัด (เหมือน VS Code)
* Checkbox ข้างบรรทัด
* ติ๊กแล้วจะไฮไลต์บรรทัดนั้น
* ดับเบิลคลิก → คัดลอกข้อความเฉพาะเนื้อหา
* แจ้งเตือนเมื่อมาร์กหรือคัดลอกสำเร็จ

### ปุ่ม “คัดลอกข้อความที่มาร์ก” (Copy Mark Button)

* ปรากฏหลังชื่อแต่ละตอน
* กดแล้วจะ:

  1. ดึงรายการหมายเลขบรรทัดที่มาร์ก
  2. ใช้ไปดึงข้อความจริงตามบรรทัด
  3. รวมข้อความทั้งหมด
  4. คัดลอกไปยัง clipboard
  5. แสดงแจ้งเตือน

### การเก็บข้อมูล (รูปแบบ JSON)

* ตาราง `line_marks_json`
* ตัวอย่าง: `[3,7,15,21,45,48]`
* เก็บในรูปแบบ array ต่อผู้ใช้ ต่อ chapter\_id
* ใช้ฟิลด์เดียว: `marked_lines_json: number[]`

### การบันทึก

* autosave ทุกครั้งที่ติ๊ก checkbox
* โหลดค่าที่เคยมาร์กไว้ เมื่อเปิดตอนใหม่
* sync ได้ข้ามอุปกรณ์ (Supabase realtime หรือ polling)
* Undo / Redo บันทึกฝั่ง client

---

## 🗃 โครงสร้างฐานข้อมูล (Supabase)

### Table: `line_marks`

```sql
create table line_marks (
  id uuid primary key default uuid_generate_v4(),
  user_id uuid references auth.users on delete cascade,
  novel_id uuid,
  chapter_id uuid,
  marked_lines_json jsonb,  -- ตัวอย่าง: [4,7,10,15,23]
  updated_at timestamp default now()
);
```

### RLS Policy

```sql
create policy "Users can access only their line marks"
on line_marks for all
using (auth.uid() = user_id);
```

### Users Table (ฟิลด์ที่ต้องมี)

* `role`: `reader` | `editor` | `admin`
* `is_vip: boolean`
* `vip_expires_at: timestamp`

---

## ⚙️ Stack ที่ใช้

* **Frontend**: Next.js, React, Tailwind CSS
* **Auth & Database**: Supabase
* **Hosting**: Vercel
* **Realtime**: Supabase Realtime / client polling

---

## 🔐 ความปลอดภัย

* ต้องล็อกอินก่อนทุกหน้า
* ทุกการมาร์ก, บุ๊คมาร์ก, การกระทำผูกกับ user
* เช็ก role ทั้งที่ frontend และ backend
* ใช้ RLS ป้องกันข้อมูลรั่วข้าม user

---

## 🛠 พัฒนาเพิ่มเติมในอนาคต

* ระบบ export/migrate line marks
* ระบบ approve ตอนแปลหลายขั้นตอน
* ระบบค้นหาบรรทัดที่มาร์กไว้
* markdown preview สำหรับ editor

---

> © 2025 Kunpeng Novel. เขียนเพื่ออิสระของนักแปล คุณภาพเพื่อผู้อ่าน
